name: Cross-Platform CI
on:
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Configure Git (Required Aider)
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Install dependencies
      run: uv sync

    - name: Install Playwright Browsers
      run: uv run playwright install chromium

    - name: Run Test Suite
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        LITELLM_API_KEY: ${{ secrets.LITELLM_API_KEY }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        # Auto-answer Aider prompts in CI
        AIDER_YES: "1"
        AIDER_NO_GITIGNORE: "1"
        # Standard flag to let tests know they are in cloud CI
        CI: "true"
      run: |
        uv run --with pytest pytest -s src/aider_factory/tests/ \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_docling_pipeline.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_max_chat_history_tokens.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_architect_summarization.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/persistant-aider/test_persistent_aider_e2e.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_workflow_smoke.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_real_session_lifecycle.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_local_persistent_pipeline.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_shared_history_live.py" \
          --ignore="src/aider_factory/tests/aider_factory_tests/end-to-end/test_e2e_shared_history_and_prompt_isolation.py" \
          -v
